# artifex
Artifex is a command-line tool for locally generating images using Stable Diffusion XL. It supports GPU/CPU inference with YAML configuration, custom prompts, and automatically timestamps generated images for easy batch workflows.
